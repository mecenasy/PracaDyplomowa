import { combineReducers, Reducer } from "redux";
import { ApplicationState, ApplicationAction } from "./constance";

export const rootReducer: Reducer<ApplicationState, ApplicationAction> = combineReducers({

})